# https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - name: ls-lint
      run: pnpm exec ls-lint

    - name: stylua
      glob: "*.lua"
      run: pnpm exec stylua {staged_files}
